<!DOCTYPE html>
<html>
<head>
  <title>Ob</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <style>
    .month { float: left; width: 24%; margin-right: 1%;}
    .horiz-bar-chart {list-style: none; padding: 0 }
    .horiz-bar-chart li {margin-bottom: 2px;}
    .horiz-bar-chart .bar {display: inline-block;}
    .horiz-bar-chart .details {color: #fff; margin: 5px; float: right}
    .horiz-bar-chart .dr {border-bottom: 2px solid #B10101;}
    .horiz-bar-chart .cr {border-bottom: 2px solid #68C502;}
    .horiz-bar-chart .in .bar {background: #68C502;}
    .horiz-bar-chart .out .bar {background: #B10101;}
  </style>
  <nav class="navbar navbar-inverse" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex8-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="#">Offline Banking</a>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse navbar-ex8-collapse">
    <ul class="nav navbar-nav">
      <% Account.all.each do |account| %>
        <li>
        <%= link_to account.number, account_transactions_path(account) %>
        </li>
      <% end %>
    </ul>
  </div><!-- /.navbar-collapse -->
  </nav>

<%= yield %>

</body>
</html>
